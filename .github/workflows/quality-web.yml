name: Quality checks

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'sw.js'
      - 'manifest.webmanifest'
      - 'team-app-next/**'
      - '.github/workflows/quality-web.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'sw.js'
      - 'manifest.webmanifest'
      - 'team-app-next/**'
      - '.github/workflows/quality-web.yml'

permissions:
  contents: read

jobs:
  web-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Validate manifests
        shell: bash
        run: |
          node -e "JSON.parse(require('fs').readFileSync('manifest.webmanifest','utf8')); console.log('manifest root OK')"
          node -e "JSON.parse(require('fs').readFileSync('team-app-next/manifest.webmanifest','utf8')); console.log('manifest V2 OK')"

      - name: Validate service workers
        shell: bash
        run: |
          node --check sw.js
          node --check team-app-next/sw.js

      - name: Validate V2 JavaScript
        shell: bash
        run: |
          for f in team-app-next/app*.js; do
            echo "Checking $f"
            node --check "$f"
          done

      - name: Validate inline JavaScript syntax
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          html = Path('index.html').read_text(encoding='utf-8')
          scripts = re.findall(r'<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>', html, flags=re.S|re.I)
          if not scripts:
              raise SystemExit('Aucun script inline trouvé')
          for i, script in enumerate(scripts, 1):
              Path(f'/tmp/inline-{i}.js').write_text(script, encoding='utf-8')
          print(f'{len(scripts)} script(s) inline extrait(s)')
          PY
          for f in /tmp/inline-*.js; do node --check "$f"; done

      - name: Reject unresolved merge markers
        shell: bash
        run: |
          if grep -R -n -E '^(<<<<<<<|=======|>>>>>>>)' index.html sw.js manifest.webmanifest team-app-next; then
            echo 'Marqueur de conflit Git détecté'
            exit 1
          fi
